[user]
	name = jonafll
	email = 42044390+jonafll@users.noreply.github.com
	signingKey = FE9D061478D5ECE5

[init]
	defaultBranch = main

[commit]
	gpgsign = true

[tag]
	gpgSign = true

[alias]
	st = status
	cm = commit -m
	cma = commit -am
	co = checkout
	cob = checkout -b
	sw = switch
	swc = switch -c
	rs = restore
	brdel = branch -D
	rv = remote -v
	a = add
	aa = add -A
	resh = reset --hard
	pu = push
	puf = push --force
	br = branch -vv
	unstage = reset HEAD --
	last = log -1 HEAD --stat
	undo = reset HEAD~1 --mixed
	ls = log --oneline
	lal = log --oneline --graph --branches
	conf = config --global -l
	wip = commit -am WIP --no-verify
	get = cherry-pick --no-commit
	
[pull]
	ff = only
